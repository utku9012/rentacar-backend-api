apiVersion: v1
kind: Secret
metadata:
  name: rentacar-gitops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: REPLACE_WITH_GITOPS_REPOSITORY_URL
  username: REPLACE_WITH_GITHUB_APP_OR_TOKEN_USER
  password: REPLACE_WITH_TOKEN_CREATED_OUTSIDE_GIT
